import PublicProfilePage from './public-profile'

/** Direct URL public Manifest (`/manifest/:username`) — not Outpost. */
export default function PublicManifestPage() {
  return <PublicProfilePage view="manifest" />
}
